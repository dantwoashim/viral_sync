{
  "name": "viral-sync-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app",
    "clients",
    "cranks",
    "launch",
    "relayer",
    "server/actions"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:program": "anchor build",
    "lint": "npm run lint --workspaces --if-present",
    "test:protocol": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run lint --workspace app && npm run build --workspace app && npm run build --workspace viral-sync-clients && npm run build --workspace viral-sync-launch && npm run build --workspace relayer && npm run build --workspace cranks && npm run build --workspace viral-sync-actions && cargo check --manifest-path programs/viral_sync/Cargo.toml && npm run build:program && npm run test:protocol"
  },
  "overrides": {
    "axios": "1.15.2",
    "postcss": "8.5.10",
    "mocha": {
      "diff": "8.0.3",
      "serialize-javascript": "7.0.5"
    }
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@types/chai": "^5.2.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.2",
    "chai": "^4.5.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
