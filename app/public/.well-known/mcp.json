{
  "name": "viral-sync-poc1",
  "version": "1.0.0",
  "description": "Agent-readable POC-1 proof endpoints for receipt-backed Solana payouts and x402-paid outcome settlement tools.",
  "tools": [
    {
      "name": "campaign_action_metadata",
      "endpoint": "GET /api/actions/campaign/{slug}",
      "payment": "none"
    },
    {
      "name": "create_visit_pass",
      "endpoint": "POST /api/product-loop/claim-pass",
      "payment": "none"
    },
    {
      "name": "confirm_visit_pass",
      "endpoint": "POST /api/product-loop/terminal/confirm",
      "payment": "none"
    },
    {
      "name": "verify_receipt_action",
      "endpoint": "GET /api/actions/causal-receipt/{id}",
      "payment": "none"
    },
    {
      "name": "agent_receipt_context",
      "endpoint": "GET /api/agent/receipt/{id}",
      "payment": "none",
      "description": "Returns the receipt, verifier, fraud gauntlet, lineage, artifact, and x402 routing context an agent needs before trusting a payout."
    },
    {
      "name": "merchant_validation_context",
      "endpoint": "GET /api/agent/validation",
      "payment": "none",
      "description": "Returns the permissioned merchant-evidence status and explicitly says whether live traction may be claimed."
    },
    {
      "name": "world_class_readiness",
      "endpoint": "GET /api/agent/readiness",
      "payment": "none",
      "description": "Returns the phase 6-10 readiness gate across economics, security, pilot ops, demo narrative, and final judge claims."
    },
    {
      "name": "x402_create_campaign",
      "service": "viral-sync-relayer",
      "endpoint": "POST /campaigns/create",
      "payment": {
        "protocol": "x402",
        "amount": "0.10",
        "asset": "USDC",
        "network": "solana-devnet"
      }
    },
    {
      "name": "x402_verify_receipt",
      "service": "viral-sync-relayer",
      "endpoint": "GET /receipts/{receiptPda}/verify",
      "payment": {
        "protocol": "x402",
        "amount": "0.001",
        "asset": "USDC",
        "network": "solana-devnet"
      }
    }
  ],
  "proofContract": {
    "verifiedReceiptRequires": [
      "terminalVerified",
      "visitorVerified",
      "lineageVerified",
      "settlementVerified",
      "nullifierVerified",
      "fraudGauntletBlockedAllCases",
      "sourceHashesMatched"
    ],
    "currentFraudGauntlet": "19/19",
    "settlementModel": "Merchant escrow releases only after terminal and visitor co-sign the receipt."
  }
}
