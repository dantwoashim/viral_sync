# Day 211 - Choose POS/Import Path

Selected path:

```text
CSV import first, webhook-compatible mapping next.
```

Reason: pilot merchants can export CSV today. Webhooks stay behind provider-specific auth and sandbox access.
