# Day 212 - POS Adapter Skeleton

Adapter skeleton includes:

- Signed import token auth.
- Merchant/source config.
- CSV upload and signed webhook modes.
- Field mapping for receipt id, amount, and paid timestamp.
- Sandbox checks for auth, merchant lookup, and required field mapping.
