# Day 184 - SDK Package

Added workspace package:

```text
viral-sync-sdk
```

The package exposes typed receipt verification, graph, invite action, PDA seed, and webhook signature helpers.

Build command:

```bash
npm run build --workspace viral-sync-sdk
```
