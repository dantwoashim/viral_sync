# Day 288 - SDK Surface V2

The SDK surface is scoped to `verifyReceipt`, `fetchGraph`, `buildInviteAction`, and PDA helper seeds. Public helpers stay read-heavy and do not expose merchant mutation paths.
